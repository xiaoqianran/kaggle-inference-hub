[project]
name = "kaggle-inference-hub"
version = "0.5.0"
description = "Local control plane for Kaggle image generation and image-to-3D workers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=50.0.0",
    "fastapi>=0.141.1",
    "python-multipart>=0.0.32",
    "uvicorn[standard]>=0.52.3",
]

[dependency-groups]
dev = ["httpx2"]
